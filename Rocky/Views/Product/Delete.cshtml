@model Rocky_Models.ViewModels.ProductVM

@{
    var title = "Удаление товаров";
}
<form method="post" asp-action="Delete" >
    <div class="border p-3">
        <div class="form-group row">
            <h2 class="text-info pl-3">@title</h2>
        </div>
        <div class="row d-flex">
            <div class="col-8">
                <div class="form-group row mt-2">
                    <div class="col-4">
                        <label asp-for="Product.Name">Наименование</label>
                    </div>
                    <div class="col-8">
                        <input asp-for="Product.Name"  disabled class="form-control" value="@Model.Product.Name" />
                    </div>
                </div>

                <div class="form-group row mt-2">
                    <div class="col-4">
                        <label asp-for="Product.Price">Цена</label>
                    </div>
                    <div class="col-8">
                        <input asp-for="Product.Price" disabled  class="form-control" value="@Model.Product.Price" />
                    </div>
                </div>

                <div class="form-group row mt-2">
                    <div class="col-4">
                        <label asp-for="Product.Description">Описание</label>
                    </div>
                    <div class="col-8">
                        <textarea asp-for="Product.Description" disabled class="form-control summernote" value="@Model.Product.Description"></textarea>
                    </div>
                </div>

                <div class="form-group row mt-2">
                    <div class="col-4">
                        <label asp-for="Product.CategoryId"></label>
                    </div>
                    <div class="col-8">
                        <select asp-for="Product.CategoryId" disabled  asp-items="@Model.CategorySelectList" class="form-control">
                            <option disabled selected>--Select Category</option>
                        </select>
                    </div>
                </div>

                <div class="form-group row mt-2">
                    <div class="col-8 offset-4 d-flex">
                        <div class="col-4 m-2 p-2 ">
                            <input type="submit" class="btn btn-primary w-100" value="Удаление" />
                        </div>
                        <div class="col-4 m-2 p-2 ">
                            <a asp-controller="Category" asp-action="Index" class="btn btn-success w-100">К списку</a>
                        </div>
                    </div>
                </div>
            </div>
            @if (Model.Product.Id != 0)
            {
                <div class="col">
                    <img src="@Rocky_Utility.WC.ImagePath@Model.Product.Image"  style="border-radius:5px;border:1px solid #bbb;" class="w-100" />
                </div>

            }
        </div>

    </div>
</form>


@section Scripts{
    @{
        <partial name="_ValidationScriptsPartial" />
    }

    <script>

        $(document).ready(function () {
            $('.summernote').summernote(
                {height:250});
        });
    </script>

}